<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhyea.argo.data.mapper.adm.UserMapper">

    <select id="findList" resultType="com.zhyea.argo.data.entity.adm.UserEntity">
        select * from ag_adm_user
        where 1=1
        <if test="keyword != null and keyword != ''">
            and (username like concat('%',#{keyword},'%') or nickname like concat('%',#{keyword},'%'))
        </if>
        order by id desc
        limit #{offset}, #{size}
    </select>

    <select id="count" resultType="long">
        select count(*) from ag_adm_user
        where 1=1
        <if test="keyword != null and keyword != ''">
            and (username like concat('%',#{keyword},'%') or nickname like concat('%',#{keyword},'%'))
        </if>
    </select>

</mapper>