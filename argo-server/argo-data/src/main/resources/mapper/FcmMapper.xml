<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.zhyea.argo.data.mapper.cms.FcmMapper">


    <!-- 根据应用ID和关键字查询组件模板信息 -->
    <select id="query" resultType="com.zhyea.argo.data.entity.cms.FcmEntity">
        select * from ag_cms_fcm where deleted=0
        <if test="null != appCode">
            and app_code=#{appCode}
        </if>

        <if test="null != keyword and keyword != ''">
            and (
            name like concat('%', #{keyword}, '%')
            or remark like concat('%', #{keyword}, '%')
            )
        </if>
    </select>


    <!-- 根据uniqCode统计组件模板信息 -->
    <select id="countByUniqCode" resultType="java.lang.Integer">
        select count(id) as cnt from ag_cms_fcm where deleted=0 and uniq_code=#{uniqCode}
        <if test="null != id">
            and id &lt;&gt; #{id}
        </if>
    </select>


</mapper>