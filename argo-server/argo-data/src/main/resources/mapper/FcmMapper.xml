<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.zhyea.argo.data.mapper.cms.FcmMapper">


    <!-- 根据应用ID和关键字查询组件模板信息 -->
    <select id="query"
            resultType="com.zhyea.argo.data.dto.FcmDto">
        select f.*, a.app_name from
        ag_cms_fcm f left join ag_cms_app a on f.app_code=a.app_code
        where f.deleted=0
        <if test="null != appCode">
            and f.app_code=#{appCode}
        </if>

        <if test="null != keyword and keyword != ''">
            and (
            f.name like concat('%', #{keyword}, '%')
            or f.remark like concat('%', #{keyword}, '%')
            )
        </if>
    </select>

</mapper>